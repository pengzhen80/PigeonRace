
def makeHeading(lan,lon,lan_next,lon_next):
    x = lan_next - lan
    y = lon_next - lon
 
 
import numpy as geek
if __name__ == '__main__':

    # in_list =[2.0, 1.0j, 1 + 1j]
    
    # print ("Input  list : ", in_list)
    y = 1+1j
        
    out_angle = geek.angle(y, deg = True) 
    print ("output angle in degrees : ", out_angle)